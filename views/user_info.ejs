<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <title>使用者資訊</title>
</head>
<body>
  <% if (user) { %>
    <h2>使用者資訊</h2>
    <p><strong>UID:</strong> <%= user.uid %></p>
    <p><strong>帳號:</strong> <%= user.usrname %></p>
    <p><strong>身份:</strong> <%= user.usr_group %></p>
  <% } else { %>
    <p>尚未登入</p>
  <% } %>
</body>
</html>
